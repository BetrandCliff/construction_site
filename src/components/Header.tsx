'use client';
import Link from 'next/link';
import {Menu, X} from 'lucide-react';
import {useState} from 'react';
export default function Header(){const [open,setOpen]=useState(false);const links=[['Home','/'],['Projects','/projects'],['3D Designs','/3d-designs'],['Services','/services'],['About','/about'],['Contact','/contact']];return <header className="sticky top-0 z-50 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="container flex h-18 items-center justify-between"><Link href="/" className="flex items-center gap-2 font-black text-slate-950"><span className="grid size-9 place-items-center rounded-lg bg-[#081a2c] text-white">◆</span><span>Build<span className="text-[#147ee8]">Vision</span></span></Link><nav className="hidden items-center gap-7 md:flex">{links.map(([label,href])=><Link key={href} href={href} className="text-sm font-bold text-slate-600 hover:text-[#147ee8]">{label}</Link>)}</nav><div className="hidden md:block"><Link href="/booking" className="btn btn-primary">Book Appointment</Link></div><button className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t bg-white p-5 md:hidden"><div className="container grid gap-4">{links.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="font-bold">{label}</Link>)}<Link href="/booking" className="btn btn-primary">Book Appointment</Link></div></nav>}</header>}

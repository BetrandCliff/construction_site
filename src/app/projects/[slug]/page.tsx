import {notFound} from 'next/navigation';import Link from 'next/link';import {ArrowLeft,Maximize2,Rotate3D,ZoomIn} from 'lucide-react';import {projects} from '@/lib/data';
export default async function ProjectDetails({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)return notFound();return <div><div className="container py-7"><Link href="/projects" className="flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/>Back to Projects</Link></div><div className="container overflow-hidden rounded-3xl"><img src={p.image} alt={p.title} className="h-[360px] w-full object-cover md:h-[540px]"/></div><section className="section pt-12"><div className="container grid gap-12 lg:grid-cols-[1.1fr_.9fr]"><div><div className="eyebrow">{p.category}</div><h1 className="mt-2 text-4xl font-black">{p.title}</h1><p className="mt-5 leading-8 text-slate-600">{p.description}</p><div className="mt-7 grid grid-cols-2 gap-3 md:grid-cols-4">{[['Location',p.location],['Year',String(p.year)],['Area',p.area],['Status',p.status]].map(([a,b])=><div className="rounded-xl bg-[#f4f7fb] p-4" key={a}><p className="text-xs text-slate-500">{a}</p><p className="mt-1 text-sm font-black">{b}</p></div>)}</div></div><div className="rounded-2xl bg-[#f4f7fb] p-5"><div className="mb-4 flex items-center justify-between"><div><p className="font-black">Interactive 3D Design</p><p className="text-xs text-slate-500">Preview concept model</p></div><div className="flex gap-2 text-slate-500"><Rotate3D size={17}/><ZoomIn size={17}/><Maximize2 size={17}/></div></div><div className="grid aspect-video place-items-center overflow-hidden rounded-xl bg-slate-900"><img src={p.image} alt="3D preview" className="h-full w-full object-cover opacity-80"/><div className="absolute"/></div><Link href="/booking" className="btn btn-primary mt-4 w-full">Book a Consultation</Link></div></div></section></div>}

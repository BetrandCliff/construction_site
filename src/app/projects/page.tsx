import PageHero from '@/components/PageHero';import ProjectCard from '@/components/ProjectCard';import {getProjects} from '@/lib/projects';
export default async function Projects(){const projects=await getProjects();return <><PageHero title="Our Projects" subtitle="Explore completed, ongoing and conceptual construction projects."/><section className="section"><div className="container"><div className="mb-8 flex flex-wrap gap-2">{['All','Residential','Commercial','Modern','Traditional'].map((x,i)=><button key={x} className={`public-filter rounded-full border px-4 py-2 text-xs font-bold ${i===0?'is-active':''}`}>{x}</button>)}</div><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{projects.map(p=><ProjectCard key={p.slug} project={p}/>)}</div></div></section></>}
